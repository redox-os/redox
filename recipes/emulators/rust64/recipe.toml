[source]
git = "https://gitlab.redox-os.org/redox-os/rust64.git"
#git_upstream = "https://github.com/kondrak/rust64.git"
shallow_clone = true
[build]
template = "cargo"
dependencies = [
    "orbital"
]

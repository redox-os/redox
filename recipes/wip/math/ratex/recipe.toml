#TODO compile and test
[source]
git = "https://github.com/erweixin/RaTeX"
shallow_clone = true
[build]
template = "cargo"
cargopackages = [
    "ratex-render",
    "ratex-pdf",
    "ratex-svg",
]
cargoflags = ["--features cli embed-fonts"]

#TODO require revirt-u (our linux kvm equivalent proposal)
[source]
git = "https://github.com/superradcompany/microsandbox"
shallow_clone = true
[build]
template = "cargo"
cargopackages = ["microsandbox-cli"]
cargoflags = ["--no-default-features"]

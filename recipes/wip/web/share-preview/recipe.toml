#TODO not compiled or tested
[source]
git = "https://github.com/rafaelmardojai/share-preview"
shallow_clone = true
[build]
template = "meson"
dependencies = [
    "glib",
    "gtk4",
    "libadwaita",
]

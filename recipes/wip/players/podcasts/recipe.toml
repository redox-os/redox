#TODO not compiled or tested
[source]
git = "https://gitlab.gnome.org/World/podcasts"
shallow_clone = true
[build]
template = "meson"
dependencies = [
    "gtk4",
    "gstreamer",
    "libadwaita",
]

#TODO compile and test
#TODO fix dlopen to use fontconfig dependency
[source]
git = "https://github.com/lusingander/sauva"
shallow_clone = true
[build]
template = "cargo"
#dependencies = ["fontconfig"]

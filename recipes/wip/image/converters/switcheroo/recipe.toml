#TODO compile and test
[source]
git = "https://gitlab.com/adhami3310/Switcheroo"
shallow_clone = true
[build]
template = "meson"
dev-dependencies = [
    "host:blueprint",
]
dependencies = [
    "gtk4",
    "libadwaita",
]

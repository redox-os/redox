#TODO fs2 crate error
[source]
git = "https://github.com/webonnx/wonnx"
shallow_clone = true
[build]
template = "custom"
dependencies = [
    "openssl3",
]
script = """
DYNAMIC_INIT
cookbook_cargo_packages wonnx-cli
"""

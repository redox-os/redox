#TODO not compiled or tested
[source]
git = "https://github.com/pjd/pjdfstest"
shallow_clone = true
script = """
autotools_recursive_regenerate
"""
[build]
template = "configure"
[package]
dependencies = ["perl5"]

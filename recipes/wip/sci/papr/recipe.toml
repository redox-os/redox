#TODO compile and test
[source]
git = "https://github.com/AfrozSaqlain/Papr"
shallow_clone = true
[build]
template = "cargo"
cargopackages = ["papr-tui"]
cargoflags = ["--no-default-features"]
#dependencies = ["sqlite3"] # needed?
[package]
dependencies = ["xdg-utils"]

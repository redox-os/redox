#TODO not compiled or tested
[source]
git = "https://github.com/RainbowCookie32/rusty-psn"
shallow_clone = true
[build]
template = "cargo"
cargoflags = "--features cli"
dependencies = [
    "openssl3",
]

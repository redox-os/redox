#TODO compile and test
[source]
git = "https://github.com/ValveSoftware/GameNetworkingSockets"
rev = "v1.6.0"
shallow_clone = true
[build]
template = "cmake"
dependencies = [
    "openssl3",
    "protobuf",
]

#TODO compile and test
[source]
tar = "https://mirror.download.kiwix.org/release/libkiwix/libkiwix-14.2.1.tar.xz"
[build]
template = "meson"
dependencies = [
    "libzim",
    "libicu",
    "pugixml",
    "mustache",
    "curl",
    "microhttpd",
    "zlib",
]
[package]
dependencies = ["aria2"]

#TODO can build, not tested
[source]
git = "https://gitlab.redox-os.org/redox-os/libtool"
branch = "v2.5.4-redox"
shallow_clone = true
script = """
./bootstrap \
    --skip-po \
    --force \
    --gnulib-srcdir=./gnulib
"""

[build]
template = "configure"

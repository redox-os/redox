#TODO not compiled or tested
[source]
tar = "https://dev-www.libreoffice.org/src/libcdr/libcdr-0.1.8.tar.xz"
[build]
template = "configure"
dependencies = [
    "boost",
    "libicu",
    "liblcms",
    "librevenge",
    "zlib",
]

# TODO compiles but needs testing
[source]
git = "https://github.com/pypa/trove-classifiers"
shallow_clone = true

[build]
template = "python"
dev-dependencies = [
  "host:python312",
  "python312",
  "python3-setuptools",
  "python3-calver",
]

[package]
dependencies = [
  "python312"
]

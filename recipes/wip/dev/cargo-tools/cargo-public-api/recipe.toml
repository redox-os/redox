#TODO camino crate error
[source]
git = "https://github.com/Enselic/cargo-public-api"
shallow_clone = true
[build]
template = "cargo"
cargopackages = ["cargo-public-api"]
dependencies = [
    "openssl3",
]

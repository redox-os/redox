#TODO compile and test
[source]
git = "https://github.com/pop-os/cosmic-applets"
shallow_clone = true
[build]
template = "cargo"
cargopackages = [
    "cosmic-applet-time",
    #"cosmic-applet-audio",
]

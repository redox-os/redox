#TODO compile and test
[source]
git = "https://github.com/pop-os/cosmic-notifications"
shallow_clone = true
[build]
template = "cargo"
cargoflags = ["--no-default-features"]
dependencies = [
    "libxkbcommon",
]

#TODO make sdl3-sys crate use sdl3 recipe
[source]
git = "https://github.com/jsgroth/jgenesis"
shallow_clone = true
[build]
template = "cargo"
cargopackages = ["jgenesis-cli"]
dependencies = ["sdl3"]

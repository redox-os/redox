#TODO endianness error in compilation
# build instructions: https://github.com/melonDS-emu/melonDS/blob/master/BUILD.md#linux
[source]
git = "https://github.com/melonDS-emu/melonDS"
rev = "1.0"
shallow_clone = true
[build]
template = "cmake"
cmakeflags = [
    "-DBUILD_QT_SDL=OFF"
]
dependencies = [
    "curl",
    #"libpcap", # optional?
    "libenet",
    "sdl2",
    #"qt6-base",
    #"qt6-multimedia",
    #"qt6-svg",
    "libarchive",
    "zstd",
    "faad2",
]

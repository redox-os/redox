#TODO make sdl2-sys use the sdl2 recipe (after cargo update)
[source]
git = "https://github.com/rustzx/rustzx"
shallow_clone = true
[build]
template = "cargo"
cargopackages = ["rustzx"]
dependencies = ["sdl2"]

#TODO compile and test
[source]
git = "https://codeberg.org/frofor/pass-tui"
shallow_clone = true
[build]
template = "cargo"
cargoflags = ["--no-default-features --features=xdg"]
dependencies = ["gpgme"]

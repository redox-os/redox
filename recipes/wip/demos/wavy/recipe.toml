#TODO compiled but not tested
[source]
git = "https://github.com/ardaku/wavy"
shallow_clone = true
[build]
template = "cargo"
cargoexamples = [
    "monitor",
    "play",
    "query",
    "record",
]

#TODO compile and test
[source]
git = "https://github.com/limlabs/ratatui-3d"
shallow_clone = true
[build]
template = "cargo"
cargoflags = ["--no-default-features --features=gltf,gpu"]
cargoexamples = [
    "cornell",
    "cube",
    "dna",
]

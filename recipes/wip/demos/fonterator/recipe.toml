#TODO not compiled or tested
[source]
git = "https://github.com/ardaku/fonterator"
shallow_clone = true
[build]
template = "cargo"
cargoflags = ["--features monospace-font"]
cargoexamples = [
    "main",
    "directions",
    "image",
    "raster",
]

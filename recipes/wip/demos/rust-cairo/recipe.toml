#TODO compile and test
[source]
git = "https://gitlab.redox-os.org/redox-os/rust-cairo"
shallow_clone = true
[build]
template = "cargo"
cargoexamples = ["gui"]
dependencies = [
    "cairo",
    "expat",
    "fontconfig",
    "freetype2",
    "libpng",
    "pixman",
    "zlib",
]

#TODO wry and tao crate error
[source]
git = "https://github.com/DioxusLabs/dioxus"
shallow_clone = true
[build]
template = "cargo"
cargoexamples = [
    "calculator",
    "clock",
    "counters",
    "file_explorer",
    "hello_world",
    "multiwindow",
    "window_popup",
    "window_event",
    "window_zoom",
]
cargopackages = [
    "file-explorer",
]
dependencies = ["openssl3"]

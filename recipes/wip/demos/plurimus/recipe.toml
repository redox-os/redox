#TODO compile and test
[source]
git = "https://github.com/kenianbei/plurimus"
shallow_clone = true
[build]
template = "cargo"
cargoflags = ["--features widgets,2d"]
cargoexamples = [
    "basic",
    "pong",
]

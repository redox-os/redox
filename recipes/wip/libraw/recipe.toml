#TODO Not compiled or tested
[source]
tar = "https://www.libraw.org/data/LibRaw-0.21.1.tar.gz"
[build]
template = "configure"
dependencies = [
    "zlib",
    "libjasper",
    "libjpeg",
    "liblcms",
]

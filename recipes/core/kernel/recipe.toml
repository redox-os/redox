[source]
git = "https://gitlab.redox-os.org/Ibuki.O/kernel.git"
branch = "remove-fd-allocation"

[build]
template = "custom"
script = """
make -f ${COOKBOOK_SOURCE}/Makefile
mkdir -pv "${COOKBOOK_STAGE}/usr/lib/boot"
cp -v kernel "${COOKBOOK_STAGE}/usr/lib/boot"
"""

[source]
git = "https://gitlab.redox-os.org/willnode/kernel.git"
branch = "open-external"

[build]
template = "custom"
script = """
cd "${COOKBOOK_SOURCE}"
make install BUILD="${COOKBOOK_BUILD}" DESTDIR="${COOKBOOK_STAGE}"
"""

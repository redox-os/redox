[package]
description = "Low-level package format utilities for Redox"

[source]
git = "https://gitlab.redox-os.org/redox-os/pkgar.git"

[build]
template = "cargo"
cargopackages = [
    "pkgar",
    "pkgar-keys",
]
cargoflags = [
    "--features cli"
]

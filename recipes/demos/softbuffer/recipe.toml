# NOTE: Switch to upstream once winit pump events have been upstreamed
[source]
git = "https://github.com/willnode/softbuffer.git"
branch = "libredox-demo"
shallow_clone = true
[build]
template = "cargo"
cargoexamples = [
    "animation",
    "drm",
    "fruit",
    "raytracing",
    "rectangle",
    "transparency",
    "winit",
]

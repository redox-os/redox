[source]
git = "https://gitlab.redox-os.org/redox-os/cpal.git"
branch = "redox"
shallow_clone = true
#upstream = "https://github.com/tomaka/cpal.git"
[build]
template = "cargo"
cargoexamples = ["beep"]

# Desktop config with Ion shell fixes
include = ["../desktop.toml"]

# Add Ion configuration to fix prompt and localization errors
[[files]]
path = "/home/user/.config/ion/ionrc"
data = """
export PROMPT = "redox> "
export LC_ALL = "C"
export LANG = "C"
export TERM = "vt100"
"""

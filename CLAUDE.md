This is a fork of the Redox operating system.

We are in the process of making the whole pipeline completely Rust-based; especially the Makefile is replaced with Rust Cranelift.

git commit WIP often 